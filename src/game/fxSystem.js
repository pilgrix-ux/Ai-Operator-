export function createCombatFx(THREE){const impacts=[];function burst(position,type='impact'){const color=type==='muzzle'?0xffd27a:0xffffff;const geo=new THREE.SphereGeometry(type==='muzzle'?.08:.06,6,6);const mat=new THREE.MeshBasicMaterial({color,transparent:true,opacity:1});const mesh=new THREE.Mesh(geo,mat);mesh.position.copy(position);mesh.userData.life=type==='muzzle'?.06:.18;mesh.userData.max=mesh.userData.life;impacts.push(mesh);return mesh}function update(dt,scene){for(let i=impacts.length-1;i>=0;i--){const x=impacts[i];x.userData.life-=dt;x.scale.multiplyScalar(1+dt*10);x.material.opacity=Math.max(0,x.userData.life/x.userData.max);if(x.userData.life<=0){scene.remove(x);impacts.splice(i,1)}}}return{burst,update}}
