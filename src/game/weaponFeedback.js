export class WeaponFeedback{constructor(){this.recoil=.0;this.flash=0;this.shells=0;this.lastShot=0}fire(now,power=.018){this.lastShot=now;this.recoil=Math.min(.12,this.recoil+power);this.flash=.055;this.shells++}update(dt){this.recoil*=Math.max(0,1-dt*8);this.flash=Math.max(0,this.flash-dt);return{recoil:this.recoil,flash:this.flash,shells:this.shells}}consumeShell(){if(this.shells<=0)return false;this.shells--;return true}}
